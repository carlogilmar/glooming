{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window. Note core:window:allow-start-dragging: the window is titleBarStyle Overlay, so the webview covers the native title bar and there is no native strip to grab — dragging goes through data-tauri-drag-region, which calls startDragging(). That is NOT part of core:window:default (a read-only query set), so without it the window cannot be moved at all.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "opener:default",
    "opener:allow-open-url",
    "dialog:allow-open",
    "dialog:allow-confirm",
    "clipboard-manager:allow-write-text"
  ]
}
